{
  "name": "ibriz_first",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ ibriz_first",
    "create-local": "graph create --node http://localhost:8020/ ibriz_first",
    "remove-local": "graph remove --node http://localhost:8020/ ibriz_first",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ibriz_first",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.45.0",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
